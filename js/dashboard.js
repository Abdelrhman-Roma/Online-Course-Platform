document.addEventListener("DOMContentLoaded", () => {
    const courseCount = document.getElementById("dashboard-course-count");

    if (courseCount) {
        courseCount.textContent = "3 courses";
    }
});
